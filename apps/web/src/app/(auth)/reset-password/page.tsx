import React from 'react';
import SetNewPasswordForm from '@features/auth/auth-form/set-new-password-form';
const ResetPassword = () => {
  return <SetNewPasswordForm />;
};

export default ResetPassword;
